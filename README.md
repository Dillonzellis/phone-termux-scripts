mkdir -p "$HOME/bin" && \
curl -fsSL "https://raw.githubusercontent.com/dillonzellis/phone-termux-scripts/master/orgmode/refiler-sync.sh" \
  -o "$HOME/bin/refiler-sync" && \
chmod +x "$HOME/bin/refiler-sync"
